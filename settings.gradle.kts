/*
 * ============================================================================
 *  UnlockEnchantment Gradle 设置脚本 (settings.gradle.kts)
 * ============================================================================
 *  本文件是 Gradle 构建的入口，会在任何 build.gradle.kts 之前执行。
 *  这里通过 rootProject.name 设定根项目名称，该名称会被用作
 *  构建产物（jar）的默认文件名，以及 Gradle 内部对项目的标识。
 *  由于本项目是单模块结构，settings 文件仅需配置项目名即可。
 * ============================================================================
 */

rootProject.name = "UnlockEnchantment"
